import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'altogic-auth-ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
