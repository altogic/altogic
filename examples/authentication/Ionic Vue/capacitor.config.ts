import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'altogic-auth-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
